import { Construct } from 'constructs'

export interface CdkCloudFrontStaticProps {
  // Define construct properties here
}

export class CdkCloudFrontStatic extends Construct {
  constructor(
    scope: Construct,
    id: string,
    props: CdkCloudFrontStaticProps = {},
  ) {
    super(scope, id)

    // Define construct contents here
  }
}
