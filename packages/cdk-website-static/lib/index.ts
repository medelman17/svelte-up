import { Construct } from 'constructs'

export interface CdkWebsiteStaticProps {
  // Define construct properties here
}

export class CdkWebsiteStatic extends Construct {
  constructor(scope: Construct, id: string, props: CdkWebsiteStaticProps = {}) {
    super(scope, id)

    // Define construct contents here
  }
}
