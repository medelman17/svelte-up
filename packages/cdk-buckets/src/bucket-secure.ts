import { Construct } from 'constructs'

export interface SupBucketSecureProps {
  // Define construct properties here
}

export class SupBucketSecure extends Construct {
  constructor(scope: Construct, id: string, props: SupBucketSecureProps = {}) {
    super(scope, id)

    // Define construct contents here
  }
}
